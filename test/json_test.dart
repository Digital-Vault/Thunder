@Skip('Needs to be moved to bloc')
// import 'dart:io';

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('test parsing json', () async {
    // final jsonString = await _loadJsonFile();
    // final books = await parseBooks(jsonString);

    // expect(books, isNotEmpty);
    // expect(books.first.title, "THE NEW GIRL");
  });
}

// Future<String> _loadJsonFile() async {
//   final file = File('assets/test.json');
//   return await file.readAsString();
// }
